# Used only by docker-compose for the local Postgres+PostGIS container.
# Each app (apps/api, apps/web) has its own .env.example for its runtime config.
POSTGRES_USER=transparency
POSTGRES_PASSWORD=transparency_dev_pw
POSTGRES_DB=transparency_ph
